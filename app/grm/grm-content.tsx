"use client"

import { useState } from "react"
import {
  Activity, ArrowDownRight, ArrowRight, ArrowUpRight, AudioLines, CalendarDays,
  Camera, Check, CheckCircle2, ChevronDown, ChevronLeft, ChevronRight, CircleAlert,
  ClipboardCheck, Clock3, CloudUpload, FilePenLine, FileText, Headphones, Heart,
  LayoutDashboard, ListFilter, MessageCircle, Mic, MoreHorizontal, Phone, Play,
  Plus, Search, Settings2, ShieldCheck, SlidersHorizontal, Sparkles, Star,
  TrendingUp, UserRound, Users, Video, X,
} from "lucide-react"
import styles from "./grm.module.css"

type Section = "desk" | "config" | "reports"
type DeskView = "visits" | "checkout"
type Guest = {
  room: string
  name: string
  initials: string
  stay: string
  day: string
  visits: number
  last: string
  status: "Follow-up due" | "Visit due" | "On track" | "Checkout today"
  mood: "Needs attention" | "Positive" | "Neutral"
  tone: string
}

const guests: Guest[] = [
  { room: "108", name: "Meera Nair", initials: "MN", stay: "12 Sep – 11 Oct", day: "Day 15 of 30", visits: 11, last: "Yesterday, 5:20 PM", status: "Follow-up due", mood: "Needs attention", tone: "coral" },
  { room: "204", name: "Daniel Weber", initials: "DW", stay: "18 Sep – 17 Oct", day: "Day 9 of 30", visits: 8, last: "Today, 10:40 AM", status: "On track", mood: "Positive", tone: "sage" },
  { room: "116", name: "Ananya Sharma", initials: "AS", stay: "21 Sep – 20 Oct", day: "Day 6 of 30", visits: 5, last: "Yesterday, 3:15 PM", status: "Visit due", mood: "Neutral", tone: "gold" },
  { room: "302", name: "Sofia Laurent", initials: "SL", stay: "28 Aug – 26 Sep", day: "Day 30 of 30", visits: 23, last: "Today, 9:10 AM", status: "Checkout today", mood: "Positive", tone: "sage" },
]

const timeline = [
  { time: "Yesterday · 5:20 PM", channel: "In person", title: "Follow-up on dining preference", text: "Guest appreciated the revised dinner. Asked to keep lighter evening meals for the rest of the stay.", icon: MessageCircle, tone: "sage", tag: "Resolved" },
  { time: "Yesterday · 11:15 AM", channel: "Phone call", title: "Dinner felt too heavy", text: "Guest asked for a lighter evening meal. Dining team informed; confirmation due today.", icon: Phone, tone: "coral", tag: "Follow-up due" },
  { time: "24 Sep · 4:30 PM", channel: "In person · Audio", title: "Therapy session feedback", text: "“The therapist was very attentive and explained everything clearly.”", icon: AudioLines, tone: "gold", tag: "Transcript ready" },
]

const feedbackTopics = ["Overall stay", "Treatment & care", "Food & dining", "Room comfort", "Staff & service", "Facilities"]

function Pill({ children, tone = "neutral" }: { children: React.ReactNode; tone?: string }) {
  return <span className={`${styles.pill} ${styles[`pill_${tone}`] || ""}`}>{children}</span>
}

function SectionHeading({ eyebrow, title, description }: { eyebrow: string; title: string; description?: string }) {
  return <div className={styles.sectionHeading}><span className={styles.eyebrow}>{eyebrow}</span><h2>{title}</h2>{description && <p>{description}</p>}</div>
}

export default function GrmContent({ section }: { section: Section }) {
  const [deskView, setDeskView] = useState<DeskView>("visits")
  const [selected, setSelected] = useState(guests[0])
  const [search, setSearch] = useState("")
  const [captureOpen, setCaptureOpen] = useState(false)
  const [captureMode, setCaptureMode] = useState<"type" | "audio">("type")
  const [captureSource, setCaptureSource] = useState("In person")
  const [captureMood, setCaptureMood] = useState("Good")
  const [configTab, setConfigTab] = useState("Daily workflow")
  const [checkoutStep, setCheckoutStep] = useState(1)
  const [testimonialMode, setTestimonialMode] = useState("Video")
  const [notice, setNotice] = useState("")

  const showNotice = (message: string) => {
    setNotice(message)
    window.setTimeout(() => setNotice(""), 3500)
  }

  const filteredGuests = guests.filter((guest) => `${guest.name} ${guest.room}`.toLowerCase().includes(search.toLowerCase()))

  return <div className={styles.shell}>
      <div className={styles.main}>
        {section === "desk" && <>
          <div className={styles.pageIntro}><div><span className={styles.eyebrow}>GUEST RELATIONS · LIVE STAYS</span><h1>Good morning, GRE team <span>✦</span></h1><p>A thoughtful moment with every guest, every day.</p></div><button className={styles.primaryButton} onClick={() => { setDeskView("visits"); setCaptureOpen(true) }}><Plus size={18} /> New feedback</button></div>
          <div className={styles.viewTabs}><button className={deskView === "visits" ? styles.viewActive : ""} onClick={() => setDeskView("visits")}><Activity size={17} /> Stay visits</button><button className={deskView === "checkout" ? styles.viewActive : ""} onClick={() => setDeskView("checkout")}><ClipboardCheck size={17} /> Checkout experience</button></div>
          {deskView === "visits" ? <>
            <div className={styles.statsGrid}>
              <StatCard icon={Users} label="Guests in residence" value="28" detail="Across 24 occupied rooms" color="olive" />
              <StatCard icon={Heart} label="Visited today" value="19/28" detail="68% daily coverage" color="sage" />
              <StatCard icon={Clock3} label="Follow-ups due" value="04" detail="2 need attention today" color="coral" />
              <StatCard icon={Star} label="Guest pulse" value="4.7" detail="From 96 stay interactions" color="gold" />
            </div>
            <div className={styles.deskGrid}>
              <section className={styles.guestPanel}>
                <div className={styles.panelTop}><div><span className={styles.eyebrow}>YOUR ROUNDS</span><h3>Guests to connect with</h3></div><button className={styles.softIcon} aria-label="Filter guests" onClick={() => showNotice("Filters will use live stay data in the full app.")}><ListFilter size={18} /></button></div>
                <label className={styles.searchBox}><Search size={18} /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search guest or room number" /></label>
                <div className={styles.guestList}>{filteredGuests.map((guest) => <button key={guest.room} className={`${styles.guestRow} ${selected.room === guest.room ? styles.guestSelected : ""}`} onClick={() => setSelected(guest)}>
                  <div className={`${styles.avatar} ${styles[`avatar_${guest.tone}`]}`}>{guest.initials}</div><div className={styles.guestMeta}><div><strong>{guest.name}</strong><span>Room {guest.room}</span></div><small>{guest.day} · {guest.visits} interactions</small></div><Pill tone={guest.tone}>{guest.status}</Pill><ChevronRight className={styles.rowChevron} size={17} />
                </button>)}{filteredGuests.length === 0 && <p className={styles.empty}>No sample guest matches your search.</p>}</div>
                <div className={styles.panelFoot}><span><span className={styles.liveDot} /> 4 sample guests shown</span><button onClick={() => showNotice("Full guest list is part of the connected version.")}>View all guests <ArrowRight size={15} /></button></div>
              </section>

              <section className={styles.detailPanel}>
                <div className={styles.detailHero}><div><span className={styles.eyebrow}>ROOM {selected.room} · {selected.day.toUpperCase()}</span><h3>{selected.name}</h3><p>{selected.stay} · Ayurveda wellness stay</p></div><div className={`${styles.avatarLarge} ${styles[`avatar_${selected.tone}`]}`}>{selected.initials}</div></div>
                <div className={styles.pulseStrip}><span>Current guest pulse</span><strong><span className={`${styles.pulseDot} ${styles[`pulse_${selected.tone}`]}`} /> {selected.mood}</strong><span>{selected.visits} notes during stay</span></div>
                <div className={styles.detailActions}><button className={styles.primaryButton} onClick={() => setCaptureOpen(true)}><Plus size={17} /> Add interaction</button><button className={styles.outlineButton} onClick={() => { setDeskView("checkout"); setCheckoutStep(1) }}><ClipboardCheck size={17} /> Checkout form</button></div>
                <div className={styles.timelineTitle}><h4>Stay journal</h4><span>Latest interactions</span></div>
                <div className={styles.timeline}>{timeline.map((item) => <div className={styles.timelineItem} key={item.title}><div className={`${styles.timelineIcon} ${styles[`icon_${item.tone}`]}`}><item.icon size={17} /></div><div><div className={styles.timelineMeta}><span>{item.time} · {item.channel}</span><Pill tone={item.tone}>{item.tag}</Pill></div><strong>{item.title}</strong><p>{item.text}</p></div></div>)}</div>
                <button className={styles.timelineMore} onClick={() => showNotice("The complete guest timeline will appear here with live data.")}>View full stay history <ArrowRight size={15} /></button>
              </section>
            </div>
          </> : <CheckoutPreview step={checkoutStep} setStep={setCheckoutStep} testimonialMode={testimonialMode} setTestimonialMode={setTestimonialMode} showNotice={showNotice} />}
        </>}

        {section === "config" && <ConfigPreview activeTab={configTab} setActiveTab={setConfigTab} showNotice={showNotice} />}
        {section === "reports" && <ReportsPreview showNotice={showNotice} />}
      </div>

    {captureOpen && <div className={styles.modalBackdrop} onMouseDown={(event) => { if (event.target === event.currentTarget) setCaptureOpen(false) }}><div className={styles.captureSheet} role="dialog" aria-modal="true" aria-label="New guest feedback"><div className={styles.sheetHead}><div><span className={styles.eyebrow}>ROOM {selected.room} · {selected.name.toUpperCase()}</span><h2>How was your conversation?</h2><p>Capture the moment while it is fresh.</p></div><button className={styles.softIcon} onClick={() => setCaptureOpen(false)} aria-label="Close"><X size={20} /></button></div>
      <div className={styles.sheetBody}><div className={styles.field}><label>How did you connect?</label><div className={styles.segmented}>{["In person", "Phone call", "Guest message"].map((source) => <button key={source} className={captureSource === source ? styles.segmentActive : ""} onClick={() => setCaptureSource(source)}>{source === "Phone call" && <Phone size={15} />}{source === "In person" && <UserRound size={15} />}{source === "Guest message" && <MessageCircle size={15} />}{source}</button>)}</div></div>
      <div className={styles.field}><label>Guest expression</label><div className={styles.moodRow}>{[{ value: "Delighted", icon: "✦" }, { value: "Good", icon: "☺" }, { value: "Mixed", icon: "◐" }, { value: "Needs help", icon: "!" }].map((mood) => <button key={mood.value} className={captureMood === mood.value ? styles.moodActive : ""} onClick={() => setCaptureMood(mood.value)}><span>{mood.icon}</span>{mood.value}</button>)}</div></div>
      <div className={styles.field}><label>What did the guest say?</label><div className={styles.inputTabs}><button className={captureMode === "type" ? styles.inputActive : ""} onClick={() => setCaptureMode("type")}><FilePenLine size={16} /> Type note</button><button className={captureMode === "audio" ? styles.inputActive : ""} onClick={() => setCaptureMode("audio")}><Mic size={16} /> Record audio</button></div>{captureMode === "type" ? <textarea placeholder="Write their words in a few lines…" rows={4} /> : <div className={styles.recordBox}><div className={styles.recordCircle}><Mic size={24} /></div><strong>Tap to record guest feedback</strong><span>Recording and ElevenLabs transcript in the full app</span><button onClick={() => showNotice("Recording is intentionally inactive in this design preview.")}>Start recording</button></div>}</div>
      <div className={styles.field}><label>Category & next step</label><div className={styles.twoFields}><select defaultValue="Food & dining">{feedbackTopics.map((topic) => <option key={topic}>{topic}</option>)}</select><select defaultValue="No action needed"><option>No action needed</option><option>Assign follow-up</option><option>Escalate issue</option></select></div></div></div><div className={styles.sheetFooter}><span><ShieldCheck size={16} /> Private guest record</span><button className={styles.primaryButton} onClick={() => { setCaptureOpen(false); showNotice("This is a design preview. No guest feedback was saved.") }}>Save interaction <ArrowRight size={17} /></button></div></div></div>}
    {notice && <div className={styles.toast}><CheckCircle2 size={18} /> {notice}</div>}
  </div>
}

function StatCard({ icon: Icon, label, value, detail, color }: { icon: React.ElementType; label: string; value: string; detail: string; color: string }) {
  return <div className={styles.statCard}><div className={styles.statTop}><span>{label}</span><div className={`${styles.statIcon} ${styles[`icon_${color}`]}`}><Icon size={19} /></div></div><strong>{value}</strong><small>{detail}</small></div>
}

function CheckoutPreview({ step, setStep, testimonialMode, setTestimonialMode, showNotice }: { step: number; setStep: (step: number) => void; testimonialMode: string; setTestimonialMode: (mode: string) => void; showNotice: (message: string) => void }) {
  return <div className={styles.checkoutLayout}><div className={styles.checkoutMain}><div className={styles.checkoutBanner}><span className={styles.eyebrow}>A THOUGHTFUL FAREWELL</span><h2>Every stay has a story.</h2><p>Gather the guest&apos;s final reflections in a calm, easy flow.</p></div><div className={styles.stepper}>{["Stay details", "Your experience", "Final thoughts"].map((title, index) => <button key={title} className={step === index + 1 ? styles.stepActive : step > index + 1 ? styles.stepDone : ""} onClick={() => setStep(index + 1)}><span>{step > index + 1 ? <Check size={15} /> : `0${index + 1}`}</span><strong>{title}</strong></button>)}</div>
    <div className={styles.checkoutForm}>{step === 1 ? <><SectionHeading eyebrow="STEP 01 · PRE-FILLED" title="A warm goodbye, Sofia" description="Confirm the stay before sharing the tablet with the guest." /><div className={styles.formGrid}><div className={styles.readonlyField}><label>Guest name</label><strong>Sofia Laurent</strong></div><div className={styles.readonlyField}><label>Room</label><strong>302 · Royal Villa</strong></div><div className={styles.readonlyField}><label>Stay dates</label><strong>28 Aug – 26 Sep 2026</strong></div><div className={styles.readonlyField}><label>Stay length</label><strong>30 days</strong></div></div><div className={styles.infoNote}><Sparkles size={18} /><span>23 interactions captured during this stay. The guest&apos;s final feedback will complete their experience story.</span></div></> : step === 2 ? <><SectionHeading eyebrow="STEP 02 · QUICK RATINGS" title="How did your stay feel?" description="A few thoughtful ratings help us celebrate what worked and improve what did not." /><div className={styles.ratingRows}>{feedbackTopics.map((topic, index) => <div className={styles.ratingRow} key={topic}><span>{topic}</span><div>{[1, 2, 3, 4, 5].map((star) => <button key={star} aria-label={`${topic}: ${star} stars`} onClick={() => showNotice("Rating selection is visual in this preview.")}><Star size={21} fill={star <= (index === 2 ? 4 : 5) ? "currentColor" : "none"} /></button>)}</div></div>)}</div><div className={styles.infoNote}><CircleAlert size={18} /><span>A low rating will reveal a short follow-up question in the connected form.</span></div></> : <><SectionHeading eyebrow="STEP 03 · IN THEIR OWN WORDS" title="Your voice makes us better" description="Write a note, record a testimonial, or share a handwritten page." /><div className={styles.field}><label>What made this stay memorable?</label><textarea rows={3} placeholder="A moment, a person, or an experience you would like to remember…" /></div><div className={styles.field}><label>What could we improve?</label><textarea rows={3} placeholder="Your honest suggestions help us grow…" /></div><div className={styles.testimonialChoices}>{[{ label: "Video", icon: Video }, { label: "Audio", icon: Mic }, { label: "Upload file", icon: CloudUpload }, { label: "Handwritten", icon: FileText }].map(({ label, icon: Icon }) => <button key={label} className={testimonialMode === label ? styles.testimonialActive : ""} onClick={() => setTestimonialMode(label)}><Icon size={20} /><span>{label}</span></button>)}</div><div className={styles.mediaPlaceholder}><div><div className={styles.mediaIcon}>{testimonialMode === "Video" ? <Camera size={25} /> : testimonialMode === "Audio" ? <Mic size={25} /> : <CloudUpload size={25} />}</div><strong>{testimonialMode === "Video" ? "Record a video testimonial" : testimonialMode === "Audio" ? "Record an audio message" : testimonialMode === "Handwritten" ? "Photograph a handwritten note" : "Upload your testimonial"}</strong><span>Direct capture and upload will be enabled in the connected app.</span><button onClick={() => showNotice("Media capture is intentionally inactive in this design preview.")}>{testimonialMode === "Video" ? "Open camera" : testimonialMode === "Audio" ? "Open microphone" : "Choose file"}</button></div></div><label className={styles.consentLine}><input type="checkbox" /> I agree to share my feedback with Kairali.</label><label className={styles.consentLine}><input type="checkbox" /> I separately allow Kairali to publish my testimonial.</label></>}
    <div className={styles.checkoutControls}><button className={styles.outlineButton} onClick={() => setStep(Math.max(1, step - 1))}><ChevronLeft size={17} /> Back</button><button className={styles.primaryButton} onClick={() => step < 3 ? setStep(step + 1) : showNotice("This is a design preview. No checkout feedback was submitted.")}>{step < 3 ? "Continue" : "Finish preview"} <ArrowRight size={17} /></button></div></div></div><aside className={styles.checkoutAside}><div className={styles.asideQuote}>“</div><h3>We hope you leave with something wonderful.</h3><p>Your reflections help us make every healing journey more personal.</p><div className={styles.asideDivider} /><div className={styles.asideRow}><CheckCircle2 size={18} /><span>Simple, guided steps</span></div><div className={styles.asideRow}><AudioLines size={18} /><span>Audio & video options</span></div><div className={styles.asideRow}><ShieldCheck size={18} /><span>Your privacy matters</span></div><div className={styles.asideLeaf}>✳</div></aside></div>
}

function ConfigPreview({ activeTab, setActiveTab, showNotice }: { activeTab: string; setActiveTab: (tab: string) => void; showNotice: (message: string) => void }) {
  const tabs = ["Daily workflow", "Forms & questions", "Issues & routing", "ElevenLabs", "Media & consent", "Access"]
  return <><div className={styles.pageIntro}><div><span className={styles.eyebrow}>CONTROL CENTER</span><h1>Make every interaction count.</h1><p>Shape the guest journey without changing the GRE&apos;s daily flow.</p></div><button className={styles.primaryButton} onClick={() => showNotice("Configuration changes are disabled in this design preview.")}>Publish changes <ArrowRight size={17} /></button></div><div className={styles.configLayout}><nav className={styles.configNav} aria-label="Configuration tabs">{tabs.map((tab) => <button key={tab} onClick={() => setActiveTab(tab)} className={activeTab === tab ? styles.configNavActive : ""}>{tab}<ChevronRight size={16} /></button>)}</nav><div className={styles.configContent}>{activeTab === "ElevenLabs" ? <><div className={styles.configHero}><div className={styles.configHeroIcon}><AudioLines size={25} /></div><div><span className={styles.eyebrow}>TRANSCRIPTION INTEGRATION</span><h2>ElevenLabs speech to text</h2><p>Give every recorded conversation a searchable transcript while preserving the original media.</p></div><Pill tone="gold">Not connected · preview</Pill></div><div className={styles.configCard}><div className={styles.configCardHead}><div><h3>Connection setup</h3><p>One-time admin setup; the key is never shown to GRE users.</p></div><ShieldCheck size={21} /></div><div className={styles.formGrid}><div className={styles.field}><label>API key</label><input type="password" placeholder="Paste secure API key" autoComplete="off" /><small>Stored server-side in the connected app.</small></div><div className={styles.field}><label>Transcription model</label><select defaultValue="Scribe v2"><option>Scribe v2</option></select><small>Default model can be changed by an admin.</small></div></div><div className={styles.inlineControls}><button className={styles.outlineButton} onClick={() => showNotice("Connection test is inactive in this design preview.")}>Test connection</button><span><span className={styles.statusDot} /> Awaiting secure setup</span></div></div><div className={styles.configCard}><div className={styles.configCardHead}><div><h3>Transcription preferences</h3><p>What happens after an audio or video is saved.</p></div><Settings2 size={21} /></div><div className={styles.settingRow}><div><strong>Auto-transcribe new recordings</strong><small>Queue a transcript after the original recording is stored.</small></div><span className={styles.switchOn} /></div><div className={styles.settingRow}><div><strong>Detect guest language automatically</strong><small>GRE can review and correct the resulting text.</small></div><span className={styles.switchOn} /></div><div className={styles.settingRow}><div><strong>Keep original AI transcript</strong><small>Preserve the source alongside GRE corrections.</small></div><span className={styles.switchOn} /></div></div></> : activeTab === "Daily workflow" ? <><SectionHeading eyebrow="DAILY CARE" title="Stay visit rhythm" description="A strong process that stays flexible for each guest." /><div className={styles.configCard}><div className={styles.configCardHead}><div><h3>Visit cadence</h3><p>Set the default expectation and allow extra interactions at any time.</p></div><Clock3 size={21} /></div><div className={styles.formGrid}><div className={styles.field}><label>Default touchpoint</label><select defaultValue="Daily"><option>Daily</option><option>Every 2 days</option><option>Weekly</option></select></div><div className={styles.field}><label>Missed visit reminder</label><select defaultValue="After 6 hours"><option>After 6 hours</option><option>End of day</option><option>Next morning</option></select></div></div><div className={styles.settingRow}><div><strong>Allow multiple notes per day</strong><small>Each meeting, call or message is a separate timestamped interaction.</small></div><span className={styles.switchOn} /></div><div className={styles.settingRow}><div><strong>Require guest confirmation to close an issue</strong><small>GRE verifies that the resolution actually helped.</small></div><span className={styles.switchOn} /></div></div><div className={styles.configCard}><div className={styles.configCardHead}><div><h3>Interaction channels</h3><p>Choose what the GRE can record.</p></div><MessageCircle size={21} /></div><div className={styles.channelTags}><span>✓ In person</span><span>✓ Phone call</span><span>✓ Guest message</span><span>✓ GRE observation</span></div></div></> : activeTab === "Forms & questions" ? <><SectionHeading eyebrow="DYNAMIC FORMS" title="Questions that fit the moment" description="Short daily notes and a richer checkout experience." /><div className={styles.configCard}><div className={styles.configCardHead}><div><h3>Daily feedback form</h3><p>Fast enough to use while walking between rooms.</p></div><FilePenLine size={21} /></div>{["How did you connect?", "What did the guest say?", "Guest expression", "Category & next step"].map((question, index) => <div className={styles.questionRow} key={question}><span>0{index + 1}</span><strong>{question}</strong><MoreHorizontal size={18} /></div>)}<button className={styles.textButton} onClick={() => showNotice("Question editing is inactive in this design preview.")}><Plus size={16} /> Add question</button></div><div className={styles.configCard}><div className={styles.configCardHead}><div><h3>Checkout feedback</h3><p>Reorder sections, set required fields and add conditional follow-ups.</p></div><ClipboardCheck size={21} /></div><div className={styles.channelTags}><span>Stay details</span><span>Treatments</span><span>Dining</span><span>Room & facilities</span><span>Final thoughts</span><span>Testimonial</span></div></div></> : activeTab === "Issues & routing" ? <><SectionHeading eyebrow="SERVICE RECOVERY" title="Every concern has an owner" description="Route issues quickly and verify resolution with the guest." /><div className={styles.configCard}><div className={styles.configCardHead}><div><h3>Routing rules</h3><p>Example department mapping for the preview.</p></div><Users size={21} /></div>{[["Food & dining", "Dining manager", "Within 2 hours"], ["Treatment & care", "Clinical coordinator", "Within 1 hour"], ["Room comfort", "Housekeeping", "Within 2 hours"]].map(([topic, owner, target]) => <div className={styles.routingRow} key={topic}><strong>{topic}</strong><span>{owner}</span><small>{target}</small></div>)}</div></> : activeTab === "Media & consent" ? <><SectionHeading eyebrow="TRUST & MEDIA" title="Capture stories respectfully" description="Controls for recording, uploading and testimonial consent." /><div className={styles.configCard}><div className={styles.settingRow}><div><strong>Require consent before recording</strong><small>Shown before opening the camera or microphone.</small></div><span className={styles.switchOn} /></div><div className={styles.settingRow}><div><strong>Separate publication permission</strong><small>Guests can share feedback without approving public use.</small></div><span className={styles.switchOn} /></div><div className={styles.channelTags}><span>Video testimonial</span><span>Audio message</span><span>File upload</span><span>Handwritten photo</span></div></div></> : <><SectionHeading eyebrow="PEOPLE & PERMISSIONS" title="The right view for every role" description="GRE captures care moments; management sees patterns and decisions." /><div className={styles.configCard}>{[["GRE team", "Create feedback, review assigned guests"], ["Team lead", "Resolve assigned issues, monitor follow-ups"], ["Management", "Reports, final score review, action planning"], ["Admin", "Configuration and secure integrations"]].map(([role, description]) => <div className={styles.roleRow} key={role}><div className={styles.roleIcon}><UserRound size={18} /></div><div><strong>{role}</strong><small>{description}</small></div><ChevronRight size={16} /></div>)}</div></>}</div></div></>
}

function ReportsPreview({ showNotice }: { showNotice: (message: string) => void }) {
  return <><div className={styles.pageIntro}><div><span className={styles.eyebrow}>MANAGEMENT · GUEST EXPERIENCE</span><h1>The complete guest picture.</h1><p>From daily conversations to lasting improvements.</p></div><button className={styles.outlineButton} onClick={() => showNotice("Report export is inactive in this design preview.")}><ArrowDownRight size={17} /> Export report</button></div><div className={styles.reportFilter}><span><CalendarDays size={17} /> 1 – 26 September 2026 <ChevronDown size={16} /></span><span><SlidersHorizontal size={17} /> All departments <ChevronDown size={16} /></span><span className={styles.reportCompare}><TrendingUp size={17} /> Compared with previous period</span></div><div className={styles.statsGrid}><StatCard icon={Heart} label="Guest satisfaction" value="4.7 / 5" detail="↑ 0.3 vs previous period" color="sage" /><StatCard icon={MessageCircle} label="Feedback captured" value="384" detail="Across 52 guest stays" color="olive" /><StatCard icon={Activity} label="Daily coverage" value="92%" detail="Of scheduled touchpoints" color="gold" /><StatCard icon={CircleAlert} label="Open concerns" value="07" detail="3 past follow-up target" color="coral" /></div><div className={styles.reportGrid}><section className={styles.chartCard}><div className={styles.panelTop}><div><span className={styles.eyebrow}>THE BIG PICTURE</span><h3>Guest sentiment over time</h3></div><Pill tone="sage">+8.2% this month</Pill></div><div className={styles.chartLegend}><span><i className={styles.legendOlive} /> Positive</span><span><i className={styles.legendGold} /> Mixed</span><span><i className={styles.legendCoral} /> Needs attention</span></div><div className={styles.chart}><div className={styles.chartLines}><span>100%</span><span>75%</span><span>50%</span><span>25%</span><span>0%</span></div><div className={styles.barGroup}>{[[72, 18, 10], [76, 16, 8], [70, 20, 10], [81, 13, 6], [84, 11, 5], [88, 9, 3]].map((values, index) => <div className={styles.barColumn} key={index}><div className={styles.stackedBar}><span style={{ height: `${values[2]}%` }} className={styles.barCoral} /><span style={{ height: `${values[1]}%` }} className={styles.barGold} /><span style={{ height: `${values[0]}%` }} className={styles.barOlive} /></div><small>{["Apr", "May", "Jun", "Jul", "Aug", "Sep"][index]}</small></div>)}</div></div></section><section className={styles.insightCard}><div className={styles.insightIcon}><Sparkles size={22} /></div><span className={styles.eyebrow}>WHAT NEEDS ATTENTION</span><h3>Turn feedback into better care.</h3><div className={styles.insightItem}><span className={styles.insightNumber}>01</span><div><strong>Dining flexibility</strong><p>5 guests asked for lighter evening meals. Review dinner options with the dining team.</p></div></div><div className={styles.insightItem}><span className={styles.insightNumber}>02</span><div><strong>Celebrate therapy care</strong><p>Therapist attentiveness was praised in 18 guest notes this month.</p></div></div><button onClick={() => showNotice("Action tracking will use live feedback in the full app.")}>View team action plan <ArrowRight size={16} /></button></section></div><div className={styles.reportBottom}><section className={styles.guestReport}><div className={styles.panelTop}><div><span className={styles.eyebrow}>GUEST-LEVEL DETAIL</span><h3>Every stay, in context</h3></div><button className={styles.textButton} onClick={() => showNotice("Guest report drill-down is inactive in this preview.")}>View all <ArrowRight size={16} /></button></div><div className={styles.reportTableWrap}><table className={styles.reportTable}><thead><tr><th>Guest / room</th><th>Feedback</th><th>Issues</th><th>Final pulse</th><th>Score</th></tr></thead><tbody>{[["Meera Nair", "108", "11 notes", "1 follow-up", "Needs attention", "3.8"], ["Daniel Weber", "204", "8 notes", "0 open", "Positive", "4.9"], ["Sofia Laurent", "302", "23 notes", "0 open", "Positive", "4.8"], ["Ananya Sharma", "116", "5 notes", "0 open", "Neutral", "4.2"]].map(([name, room, feedback, issue, pulse, score]) => <tr key={room}><td><div className={styles.tableGuest}><span>{name.split(" ").map((part) => part[0]).join("")}</span><div><strong>{name}</strong><small>Room {room}</small></div></div></td><td>{feedback}</td><td>{issue}</td><td><Pill tone={pulse === "Positive" ? "sage" : pulse === "Neutral" ? "gold" : "coral"}>{pulse}</Pill></td><td><strong>{score}</strong> / 5</td></tr>)}</tbody></table></div></section><section className={styles.scoreCard}><div className={styles.scoreRing}><div><strong>4.7</strong><span>OUT OF 5</span></div></div><h3>How the score is built</h3><p>Daily ratings, issue resolution and checkout feedback are shown together. Management can review the final interpretation.</p><div><span>Daily care</span><strong>40%</strong></div><div><span>Issue recovery</span><strong>25%</strong></div><div><span>Checkout reflection</span><strong>35%</strong></div></section></div></>
}
