// Generated from the existing marketing report renderer; uses design fixtures only.
(function(){const exports={};
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.whatsappTemplate = exports.displayDate = void 0;
exports.yesterdayIST = yesterdayIST;
exports.demoCompanies = demoCompanies;
exports.reportScope = reportScope;
exports.validateReportView = validateReportView;
exports.scopeLabel = scopeLabel;
exports.reportHTML = reportHTML;
exports.reportExportHTML = reportExportHTML;
// Design fixtures only. No database, delivery service or production records.
function yesterdayIST() {
    const today = new Intl.DateTimeFormat('en-CA', { timeZone: 'Asia/Kolkata', year: 'numeric', month: '2-digit', day: '2-digit' }).format(new Date());
    return new Date(Date.parse(today + 'T12:00:00Z') - 86400000).toISOString().slice(0, 10);
}
const displayDate = (date) => new Intl.DateTimeFormat('en-IN', { day: '2-digit', month: 'long', year: 'numeric', timeZone: 'UTC' }).format(new Date(date + 'T12:00:00Z')) + ' (' + new Intl.DateTimeFormat('en-IN', { weekday: 'long', timeZone: 'UTC' }).format(new Date(date + 'T12:00:00Z')) + ')';
exports.displayDate = displayDate;
const money = (n) => n.toLocaleString('en-IN', { maximumFractionDigits: 2, minimumFractionDigits: 2 });
const escape = (s) => String(s).replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]));
const percent = (n, total) => total ? Math.round(n / total * 100) + '%' : '—';
function demoCompanies(date) {
    const offset = new Date(date + 'T12:00:00Z').getUTCDate() % 5;
    return [
        { name: 'KTAHV', description: 'Lead quality & marketing returns', traffic: 1401 + offset * 20, sources: ['Whatsapp Priya', 'Facebook Priya + Insta', 'Website Priya', 'Facebook & Insta Lead Form', 'Google PPC', 'Website Contact, Consultation, Enquiry Form', 'IVR'], leads: [4, 0, 8, 92 + offset, 2, 9, 36], high: [1, 0, 4, 91 + offset, 0, 1, 1], medium: [1, 0, 0, 1, 1, 2, 2], spend: [0, 0, 0, 10225, 26288, 0, 0], sale: 573471, bookings: 2 },
        { name: 'VILARAAG', description: 'Lead quality & marketing returns', traffic: 346 + offset * 10, sources: ['Facebook + Instagram', 'Website + Google PPC', 'Anjali Menon - chat bot', 'Anjali Menon - Instagram', 'IVR', 'Others'], leads: [8 + offset, 6, 0, 0, 0, 0], high: [4 + offset, 5, 0, 0, 0, 0], medium: [2, 1, 0, 0, 0, 0], spend: [3166, 7015, 0, 0, 0, 0], sale: 182000, bookings: 1 },
        { name: 'KAPPL', description: 'Illustrative structure · source mapping pending', traffic: 620 + offset * 10, sources: ['Facebook + Instagram', 'Google PPC', 'Website', 'WhatsApp', 'IVR', 'Others'], leads: [20 + offset, 12, 7, 5, 4, 2], high: [14 + offset, 8, 4, 3, 2, 1], medium: [4, 3, 2, 1, 1, 1], spend: [6400, 8200, 0, 0, 0, 0], sale: 218000, bookings: 3 },
    ].map(c => ({ ...c, totalLeads: c.leads.reduce((a, b) => a + b, 0), totalSpend: c.spend.reduce((a, b) => a + b, 0) }));
}
const fullName = (name) => name === 'KTAHV' ? 'Kairali The Ayurvedic Healing Village' : name === 'KAPPL' ? 'Kairali Ayurvedic Products Pvt Ltd' : 'VILARAAG';
const sum = (values) => values.reduce((a, b) => a + b, 0);
const leadHeaders = ['Source', 'Total traffic', 'Leads / traffic', 'Total leads', 'High quality', 'Medium quality', 'Low quality', 'Spend (₹)', 'CAC (₹)'];
const salesHeaders = ['Source', 'Spent (₹)', 'Conversions', 'Verified sales (₹)', 'ROAS', 'CAC (₹)', 'Unverified sales (₹)', 'Cancelled booking (₹)'];
function reportScope(data, scope) {
    if (scope !== 'all' && !data.companies.some(c => c.name === scope))
        throw new Error('Invalid company');
    return data.companies.filter(c => scope === 'all' || c.name === scope);
}
function validateReportView(data, view) {
    reportScope(data, view.scope);
    const keys = new Set(data.companies.flatMap(c => [c.name + '-leads', c.name + '-sales']));
    if (!Array.isArray(view.expanded) || view.expanded.length > 6 || view.expanded.some(k => !keys.has(k)))
        throw new Error('Invalid report view');
    return view;
}
function scopeLabel(data, scope) {
    const companies = reportScope(data, scope);
    return scope === 'all' ? companies.map(c => c.name).join(' · ') : fullName(companies[0].name) + ' (' + companies[0].name + ')';
}
function leadValues(c, i) {
    const total = i === undefined, n = total ? c.totalLeads : c.leads[i], traffic = total ? c.traffic : c.trafficBySource?.[i], h = total ? sum(c.high) : c.high[i], m = total ? sum(c.medium) : c.medium[i], l = total ? (c.low ? sum(c.low) : n - h - m) : (c.low?.[i] ?? n - h - m), spend = total ? c.totalSpend : c.spend[i];
    return [total ? 'Company total' : c.sources[i], traffic ?? '—', traffic ? percent(n, traffic) : '—', c.qualityAvailable === false ? '—' : n, ...[h, m, l].map(q => c.qualityAvailable === false ? '—' : q + ' · ' + percent(q, n)), money(spend), n ? money(spend / n) : '—'];
}
function salesValues(c, i) {
    const r = i === undefined ? { source: 'Company total', spend: c.totalSpend, conversions: c.bookings, verified: c.sale, unverified: c.unverified ?? 0, cancelled: c.cancelled ?? 0 } : c.liveSales?.[i] ?? { source: c.sources[i], spend: c.spend[i], conversions: 0, verified: 0, unverified: 0, cancelled: 0 };
    const leads = i === undefined ? c.totalLeads : c.leads[i];
    return [r.source, money(r.spend), r.conversions, money(r.verified), r.spend ? (r.verified / r.spend).toFixed(2) + '×' : '—', leads ? money(r.spend / leads) : '—', money(r.unverified), money(r.cancelled)];
}
function table(headers, rows, email = false) {
    const border = 'border-bottom:1px solid #e5eae7;border-right:1px solid #edf1ed;';
    const widths = headers.length === 9 ? [20, 8, 9, 8, 11, 11, 11, 12, 10] : [20, 11, 8, 14, 8, 10, 14, 15];
    if (rows.some(row => row.length !== headers.length))
        throw new Error('Report column count mismatch');
    return `<table class="data" width="100%" cellspacing="0" cellpadding="0" style="width:100%;table-layout:fixed;border-collapse:collapse;font-family:Arial,sans-serif;font-size:${email ? '11' : '12'}px;color:#203c35"><colgroup>${widths.map(width => `<col style="width:${width}%">`).join('')}</colgroup><thead><tr>${headers.map((h, j) => `<th scope="col" style="padding:11px 8px;text-align:${j ? 'right' : 'left'};vertical-align:middle;overflow-wrap:anywhere;background:#f0f4f1;color:#53675f;${border}">${escape(h)}</th>`).join('')}</tr></thead><tbody>${rows.map((row, i) => `<tr style="background:${i % 2 ? '#f8faf8' : '#ffffff'}">${row.map((value, j) => `<td style="padding:12px 8px;vertical-align:top;overflow-wrap:anywhere;font-variant-numeric:tabular-nums;${border}text-align:${j ? 'right' : 'left'};${j ? '' : 'font-weight:600;'}">${escape(value)}</td>`).join('')}</tr>`).join('')}</tbody></table>`;
}
function overview(companies, scope) {
    const total = (fn) => sum(companies.map(fn)), leads = total(c => c.totalLeads), spend = total(c => c.totalSpend), sales = total(c => c.sale), traffic = total(c => c.traffic);
    const items = [['Total leads', String(leads)], ['Ad spend', '₹' + money(spend)], ['Verified sales', '₹' + money(sales)], ['ROAS', spend ? (sales / spend).toFixed(2) + '×' : '—']];
    return `<div class="overview-label">${scope === 'all' ? 'Consolidated performance · ' + companies.length + ' companies' : escape(companies[0].name) + ' · Company performance'}</div><table class="kpis" width="100%" role="presentation"><tr>${items.map(([label, value]) => `<td><span>${label}</span><strong>${value}</strong></td>`).join('')}</tr></table><div class="secondary"><span>Traffic <b>${traffic}</b></span><span>Conversions <b>${total(c => c.bookings)}</b></span><span>CAC <b>${leads ? '₹' + money(spend / leads) : '—'}</b></span><span>Unverified sales <b>₹${money(total(c => c.unverified ?? 0))}</b></span><span>Cancelled bookings <b>₹${money(total(c => c.cancelled ?? 0))}</b></span></div>`;
}
const css = `*{box-sizing:border-box}body{margin:0;background:#f3f5f2;color:#203c35;font-family:Arial,sans-serif}.shell{max-width:1440px;margin:auto;padding:28px}.briefing{background:linear-gradient(115deg,#102e29,#214e42);color:#fff;border-radius:20px;padding:30px 34px;box-shadow:0 14px 36px #14382b12;position:relative}.brand{font-size:12px;letter-spacing:3px;color:#d7c79f;font-weight:700}.hero-top{display:flex;align-items:center;justify-content:space-between;gap:24px;margin-top:18px}.hero-top h1{font-family:Georgia,serif;font-size:clamp(24px,2.4vw,34px);font-weight:400;margin:0 0 12px}.report-date{color:#f3dfad;font-size:17px;font-weight:600;margin:0 0 9px}.scope-label{font-size:13px;color:#d0dfd5;line-height:1.5}.controls{display:flex;gap:9px;align-items:end;flex-wrap:wrap;justify-content:flex-end}.controls label{font-size:10px;letter-spacing:1px;text-transform:uppercase;color:#d1ded5;display:flex;flex-direction:column;gap:7px}.control{border:1px solid #b6cdc14a;border-radius:8px;padding:11px 10px;background:#fff;color:#244b3c;font:12px Arial;height:39px}.control.company-select{max-width:210px}.overview-label{text-transform:uppercase;font-size:10px;letter-spacing:1.8px;color:#d7c79f;margin:26px 0 13px;padding-top:23px;border-top:1px solid #ffffff26}.kpis{table-layout:fixed;border-collapse:collapse}.kpis td{vertical-align:top;padding:0 14px 2px 0}.kpis span{display:block;color:#bfd2c5;font-size:11px;margin-bottom:9px}.kpis strong{font:clamp(18px,2.1vw,30px) Georgia,serif;white-space:nowrap;font-variant-numeric:tabular-nums}.secondary{display:flex;gap:12px 23px;flex-wrap:wrap;border-top:1px solid #ffffff20;margin-top:20px;padding-top:16px;color:#bfd2c5;font-size:11px;line-height:1.8}.secondary b{color:#f0f4ee;margin-left:4px}.toolbar{display:flex;align-items:center;justify-content:space-between;margin:22px 0;font-size:12px;color:#6c7b73}.company{background:white;border:1px solid #dce5de;border-radius:15px;margin-bottom:24px;overflow:visible}.company-head{display:flex;justify-content:space-between;align-items:center;padding:23px 26px;border-bottom:1px solid #e5eae7}.company h2{margin:0 0 6px;font-size:20px}.company p{margin:0;font-size:12px;color:#6c7b73}.company-tag{display:inline-block;padding:6px 10px;background:#eff4f0;font-size:10px;letter-spacing:1px;border-radius:6px;margin-right:12px}.report{padding:22px 26px}.report+.report{border-top:1px solid #edf1ed}.report-title{display:flex;align-items:center;gap:10px;border-radius:8px;padding:12px 14px;margin-bottom:15px}.report-title h3{margin:0;font-size:14px}.lead-heading{background:#eaf4ed;color:#256448;border-left:4px solid #3e8b65}.sales-heading{background:#edf0fb;color:#425b94;border-left:4px solid #6b83b7}.report-icon{font-size:21px}.table-scroll{overflow:auto}.data{min-width:800px}details.details>summary{list-style:none;cursor:pointer;color:#be3636;font-size:12px;font-weight:600;padding:15px 0 2px}details.details>summary::before{content:'+';margin-right:9px}details.details[open]>summary::before{content:'−'}details.details[open]>.table-scroll{margin-top:15px}summary::-webkit-details-marker{display:none}button{cursor:pointer;border:1px solid #d3ded6;background:#fff;color:#254b3e;border-radius:8px;padding:10px 13px;font:12px Arial}.action-menu{position:relative}.action-menu>summary{list-style:none;cursor:pointer;background:#fff;color:#254b3e;padding:12px 14px;border:1px solid #d3ded6;border-radius:8px;font-size:12px;white-space:nowrap}.action-options{position:absolute;top:calc(100% + 6px);right:0;background:white;padding:6px;box-shadow:0 12px 32px #122b252b;border:1px solid #dce5de;border-radius:10px;width:210px;z-index:20}.action-options button{display:block;border:0;width:100%;text-align:left;padding:12px}.action-options button:hover{background:#eef4ef}.foot{font-size:11px;color:#7b877f;line-height:1.8;padding:0 4px}.status{font-size:12px;color:#486a55;min-height:18px}[hidden]{display:none!important}@media(max-width:780px){.hero-top{align-items:start;flex-direction:column}.controls{justify-content:start}.hero-top h1{font-size:30px}}@media(max-width:650px){.shell{padding:12px}.briefing{padding:24px 18px;border-radius:14px}.kpis tr{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:18px 12px}.kpis strong{font-size:21px}.kpis span{font-size:10px}.kpis td{display:block;padding-right:0}.company-head,.report{padding:18px 14px}.controls{width:100%}.control.company-select{max-width:165px}.report-date{font-size:15px}.company-head{gap:8px}.company h2{font-size:18px}.company p{max-width:190px}.secondary{gap:8px 14px}.toolbar{gap:12px;line-height:1.5}}@media print{body{background:white}.shell{padding:0}.briefing{print-color-adjust:exact;-webkit-print-color-adjust:exact}.no-print{display:none!important}.company{break-inside:auto}.data{min-width:0;font-size:10px}tr{break-inside:avoid}.table-scroll{overflow:visible}details:not([open])>*:not(summary){display:none}}`;
function actions(scope) { return `<details class="action-menu no-print"><summary>Export & share ⌄</summary><div class="action-options">${[['print', 'Print / Save PDF'], ['jpg', 'Download JPG'], ['download', 'Download HTML'], ['email', 'Email template'], ['whatsapp', 'WhatsApp share']].map(([action, label]) => `<button data-action="${action}" data-scope="${scope}">${label}</button>`).join('')}</div></details>`; }
function reportHTML(date, live) {
    const data = live ?? { companies: demoCompanies(date) }, companies = data.companies;
    return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Marketing Daily Report · ${escape((0, exports.displayDate)(date))}</title><style>${css}</style></head><body><main class="shell"><header class="briefing"><div class="brand">KAIRALI GROUP · MANAGEMENT BRIEFING</div><div class="hero-top"><div><h1>Marketing Daily Report</h1><p class="report-date">${escape((0, exports.displayDate)(date))}</p><div class="scope-label" id="scope-label">${escape(scopeLabel(data, 'all'))}</div></div><div class="controls no-print"><label>Report date<input id="report-date" class="control" type="date" value="${date}" max="${yesterdayIST()}"></label><label>Company<select id="company-filter" class="control company-select" aria-label="Select company"><option value="all">All companies</option>${companies.map(c => `<option value="${c.name}">${c.name}</option>`).join('')}</select></label>${actions('all')}</div></div>${['all', ...companies.map(c => c.name)].map(scope => `<section data-overview="${scope}" ${scope === 'all' ? '' : 'hidden'}>${overview(reportScope(data, scope), scope)}</section>`).join('')}</header><div class="toolbar no-print"><span>Summary first. Open source details to include them in your exports.</span><button id="expand">+ Expand all details</button></div>${companies.map(c => `<section class="company" data-company="${c.name}"><div class="company-head"><div><h2>${c.name}</h2><p>${escape(fullName(c.name))}</p></div>${actions(c.name)}</div>${['leads', 'sales'].map(kind => `<div class="report"><div class="report-title ${kind === 'leads' ? 'lead' : 'sales'}-heading"><span class="report-icon">${kind === 'leads' ? '◎' : '↗'}</span><h3>${kind === 'leads' ? 'Lead Quality Report' : 'Sales & Return on Ad Spend'}</h3></div><div class="table-scroll">${table(kind === 'leads' ? leadHeaders : salesHeaders, [kind === 'leads' ? leadValues(c) : salesValues(c)])}</div><details class="details" data-detail="${c.name}-${kind}"><summary>View source-wise ${kind === 'leads' ? 'lead quality' : 'sales performance'}</summary><div class="table-scroll">${table(kind === 'leads' ? leadHeaders : salesHeaders, c.sources.map((_, i) => kind === 'leads' ? leadValues(c, i) : salesValues(c, i)))}</div></details></div>`).join('')}</section>`).join('')}<footer class="foot">Prepared for Marketing Team & Management · All amounts in INR · Reporting timezone: IST<br>CAC = spend ÷ leads · ROAS = verified sales ÷ spend · — = unavailable or not applicable</footer><p id="status" class="status no-print" role="status"></p></main><script>
const labels=${JSON.stringify(Object.fromEntries(['all', ...companies.map(c => c.name)].map(s => [s, scopeLabel(data, s)]))).replace(/</g, '\\u003c')};
function currentView(scope){return {scope,expanded:[...document.querySelectorAll('details.details[open]')].map(n=>n.dataset.detail)}}
function updateExpand(){const nodes=[...document.querySelectorAll('.company:not([hidden]) details.details')];document.querySelector('#expand').textContent=nodes.every(n=>n.open)?'− Collapse all details':'+ Expand all details'}
document.querySelector('#company-filter').onchange=e=>{const scope=e.target.value;document.querySelectorAll('[data-company]').forEach(n=>n.hidden=scope!=='all'&&n.dataset.company!==scope);document.querySelectorAll('[data-overview]').forEach(n=>n.hidden=n.dataset.overview!==scope);document.querySelector('#scope-label').textContent=labels[scope];updateExpand()};
document.querySelector('#expand').onclick=()=>{const nodes=[...document.querySelectorAll('.company:not([hidden]) details.details')],open=nodes.some(n=>!n.open);nodes.forEach(n=>n.open=open);updateExpand()};document.querySelectorAll('details.details').forEach(n=>n.addEventListener('toggle',updateExpand));
document.querySelector('#report-date').onchange=e=>{if(e.target.value&&e.target.value<=e.target.max)parent.postMessage({type:'marketing-report-date',date:e.target.value},'*')};
document.querySelectorAll('[data-action]').forEach(button=>button.onclick=()=>{const scope=button.dataset.scope==='all'?document.querySelector('#company-filter').value:button.dataset.scope;button.closest('details').open=false;parent.postMessage({type:'marketing-report-export',action:button.dataset.action,view:currentView(scope)},'*')});
document.addEventListener('click',e=>document.querySelectorAll('.action-menu[open]').forEach(n=>{if(!n.contains(e.target))n.open=false}));
</script></body></html>`;
}
/** Script-free, inline-styled output shared by email, image, download and print. */
function reportExportHTML(date, data, view) {
    validateReportView(data, view);
    const companies = reportScope(data, view.scope), label = scopeLabel(data, view.scope), n = (fn) => sum(companies.map(fn)), spend = n(c => c.totalSpend), leads = n(c => c.totalLeads), sale = n(c => c.sale);
    const tiles = [['TOTAL LEADS', String(leads)], ['AD SPEND', '₹' + money(spend)], ['VERIFIED SALES', '₹' + money(sale)], ['ROAS', spend ? (sale / spend).toFixed(2) + '×' : '—']];
    const section = (c, kind) => `<h3 style="margin:20px 0 12px;padding:12px 14px;font:600 14px Arial;border-left:4px solid ${kind === 'leads' ? '#3e8b65' : '#6b83b7'};background:${kind === 'leads' ? '#eaf4ed' : '#edf0fb'};color:${kind === 'leads' ? '#256448' : '#425b94'}">${kind === 'leads' ? '◎ Lead Quality Report' : '↗ Sales & Return on Ad Spend'}</h3>${table(kind === 'leads' ? leadHeaders : salesHeaders, [kind === 'leads' ? leadValues(c) : salesValues(c)], true)}${view.expanded.includes(c.name + '-' + kind) ? `<p class="source-label" style="font:600 11px Arial;color:#66766d;margin:16px 0 8px">SOURCE-WISE DETAILS</p>${table(kind === 'leads' ? leadHeaders : salesHeaders, c.sources.map((_, i) => kind === 'leads' ? leadValues(c, i) : salesValues(c, i)), true)}` : ''}`;
    return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Marketing Daily Report · ${escape((0, exports.displayDate)(date))} · ${escape(label)}</title><style>@page{size:A4 landscape;margin:10mm 10mm 14mm;@bottom-left{content:"Marketing Daily Report · ${date} · ${view.scope === 'all' ? 'All companies' : view.scope}";font:8px Arial;color:#53675f}@bottom-right{content:"Page " counter(page) " of " counter(pages);font:8px Arial;color:#53675f}}*{box-sizing:border-box}body{margin:0}table{page-break-inside:auto}table:not([role="presentation"]) tr{break-inside:avoid;page-break-inside:avoid}thead{display:table-header-group}@media print{body{background:#fff!important}*{-webkit-print-color-adjust:exact;print-color-adjust:exact}.export-wrap{padding:0!important}.export-layout,.export-layout>tbody,.export-layout>tbody>tr,.export-layout>tbody>tr>td{display:block!important;width:100%!important;break-inside:auto!important;page-break-inside:auto!important}.export-layout{max-width:none!important}.export-layout>tbody>tr>td{padding:0!important;border:0!important;border-radius:0!important}.export-header{break-inside:avoid!important;padding:12px 18px!important;margin-bottom:12px}.export-header h1{font-size:24px!important;margin:8px 0!important}.export-header p{margin:8px 0!important;font-size:11px!important}.export-header table{margin-top:10px!important}.export-header table td div:last-child{font-size:21px!important}.export-company{margin:0 0 16px;padding:0!important;border:0!important}.export-company h2{break-after:avoid}.export-company h2+p{break-after:avoid}h3{break-after:avoid;page-break-after:avoid;margin-top:14px!important;padding:8px 10px!important}p{orphans:2;widows:2}.export-footer{display:none!important}table:not([role="presentation"]){width:100%!important;min-width:0!important}table:not([role="presentation"]) th,table:not([role="presentation"]) td{padding:7px 6px!important;font-size:10px!important}table:not([role="presentation"]) thead{break-after:avoid}.source-label{break-after:avoid}}</style></head><body style="margin:0;background:#f2f5f1;font-family:Arial,sans-serif;color:#203c35"><div class="export-wrap" style="padding:24px"><table class="export-layout" role="presentation" width="100%" cellspacing="0" cellpadding="0" style="width:100%;max-width:1320px;margin:auto"><tr><td><div class="export-header" style="padding:30px;background:#173e33;border-radius:14px;color:#fff"><div style="font:700 11px Arial;letter-spacing:2px;color:#ddc99b">KAIRALI GROUP · MANAGEMENT BRIEFING</div><h1 style="font:32px Georgia,serif;margin:17px 0 10px">Marketing Daily Report</h1><p style="font:600 17px Arial;color:#f1dbab;margin:0 0 10px">${escape((0, exports.displayDate)(date))}</p><p style="font:13px Arial;color:#cbdccf;margin:0 0 24px">${escape(label)}</p><div style="border-top:1px solid #537366;padding-top:20px;font:11px Arial;letter-spacing:1px;color:#dbc89c">${view.scope === 'all' ? 'CONSOLIDATED PERFORMANCE · ' + companies.length + ' COMPANIES' : escape(companies[0].name) + ' · COMPANY PERFORMANCE'}</div><table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="table-layout:fixed;margin-top:17px"><tr>${tiles.map(([k, v]) => `<td style="vertical-align:top;width:25%;padding-right:10px"><div style="font:10px Arial;color:#cbdccf;margin-bottom:9px">${k}</div><div style="font:26px Georgia,serif;color:#fff">${v}</div></td>`).join('')}</tr></table><p style="font:12px/1.9 Arial;color:#d6e4db;margin:22px 0 0">Traffic <b>${n(c => c.traffic)}</b> &nbsp; · &nbsp; Conversions <b>${n(c => c.bookings)}</b> &nbsp; · &nbsp; CAC <b>${leads ? '₹' + money(spend / leads) : '—'}</b><br>Unverified sales ₹${money(n(c => c.unverified ?? 0))} &nbsp; · &nbsp; Cancelled bookings ₹${money(n(c => c.cancelled ?? 0))}</p></div></td></tr>${companies.map(c => `<tr><td style="padding-top:20px"><table class="export-layout" role="presentation" width="100%" cellpadding="0" cellspacing="0"><tr><td><div class="export-company" style="background:#fff;border:1px solid #dce5de;border-radius:12px;padding:23px"><h2 style="font:700 20px Arial;margin:0 0 6px">${c.name}</h2><p style="font:12px Arial;color:#738178;margin:0">${escape(fullName(c.name))}</p>${section(c, 'leads')}${section(c, 'sales')}</div></td></tr></table></td></tr>`).join('')}<tr><td><div class="export-footer" style="font:11px/1.8 Arial;color:#77847c;padding:20px 4px">Prepared for Marketing Team & Management · All amounts in INR · Reporting timezone: IST<br>CAC = spend ÷ leads · ROAS = verified sales ÷ spend · — = unavailable or not applicable</div></td></tr></table></div></body></html>`;
}
exports.whatsappTemplate = 'Marketing Daily Report — {{1}}\nCompany: {{2}}\nPlease find the attached report for the selected date.\nKairali Group | Marketing Team';

window.MarketingReport=exports;})();